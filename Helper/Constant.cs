namespace Tenor.Helper
{
    public class Constant
    {
        public enum enOPerationTypes { counter, function, kpi, opt, voidFunction }
        public enum enAggregation { min, mix, sum, avg, count }
    }
}
